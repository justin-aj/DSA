class Solution:
    def climbStairs(self, n: int) -> int:
        hash_map = {}
        hash_map[0] = 0
        hash_map[1] = 1
        hash_map[2] = 2
        for i in range(3, n + 1):
            hash_map[i] = hash_map[i - 1] + hash_map[i - 2]
        return hash_map[n]
